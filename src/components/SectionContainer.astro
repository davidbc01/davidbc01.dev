---
interface Props {
    id: string;
    className: string;
}

const { id, className } = Astro.props;
---

<section id={id} class=`section ${className} w-full mx-auto container lg:max-w-4xl md:max-w-2xl`>
    <slot />
</section>
